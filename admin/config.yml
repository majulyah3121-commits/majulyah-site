backend:
  name: git-gateway
  branch: principal

media_folder: "images"
public_folder: "/images"

collections:
  - name: "pages"
    label: "Pages"
    files:
      - file: "index.html"
        label: "Page d'accueil"
        name: "home"
        fields:
          - {label: "Titre", name: "title", widget: "string"}
